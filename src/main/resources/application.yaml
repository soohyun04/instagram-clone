spring:
  application:
    name: instagram-clone
  config:
    import: ./config.yaml
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
  datasource:
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    encoding:
      charset: utf-8
      force: true
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    password: ${mail.password}
    username: ${mail.username}

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    port: 587
mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

file:
  upload:
    path: ${upload.profile-path}
    board-path: ${upload.board-path}

jwt:
  secret: ${jwt.config.secret}
  access-expiry: 1800000
  refresh-expiry: 1209600000

kakao:
  client-id: 9311861216c67fe70b0913f2f1987abe
  redirect-uri: http://localhost:8080/api/kakao/callback
  client-secret: w63pTtM6n4p1Poj2UqQe4QNkiVlpd2T0

logging:
  level:
    com.project.instagramclone: DEBUG # 내 프로젝트
    org.mybatis: DEBUG                # MyBatis
    #java.sql: DEBUG                   # SQL 작성문
    #java.sql.ResultSet: DEBUG         # SQL 사용에 따른 결과
    root: info
    #com.zaxxer.hikari: DEBUG
    #org.springframework.jdbc: DEBUG